#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>

pthread_t p[3];

void *child1(void *arg) {
  
	printf("\nchild was born\n");
        while(1);
}

int main(int argc, char** argv) {
        int err;
        if ( err = pthread_create(&p[0], NULL, child1, NULL) )
                printf("child was not born\n");

        printf("This is a parent\n");
        //sleep(10);
        //exit(0);
        while(1);
}
